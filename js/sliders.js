//Sliders
const swiper = new Swiper(".partners__slider", {
  // Optional parameters
  breakpoints: {
    320: {
      slidesPerView: 3,
      centeredSlides: true,
      spaceBetween: 32,
      grabCursor: true,
    },
  },
});
